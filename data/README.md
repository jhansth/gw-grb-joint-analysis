# Data Folder

This folder holds all data outputs created by the pipeline.
All files here are generated and can be safely deleted and regenerated.

Subfolders:
- `simulated/`: raw simulated trigger CSVs
- `results/`: coincidence tables and summary stats
